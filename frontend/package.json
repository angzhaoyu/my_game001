{
  "name": "farm-minigame-client-source",
  "private": true,
  "scripts": {
    "typecheck": "npm run typecheck:core && npm run typecheck:farm",
    "typecheck:core": "tsc -p tsconfig.core.json --noEmit",
    "typecheck:farm": "tsc -p tsconfig.farm.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
